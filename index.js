// tem file

const file = require('./node/file');
const math = require('./node/math');
const os = require('./node/os');
const object = require('./node/object');

module.exports = {
  file,
  math,
  os,
  object,
};
